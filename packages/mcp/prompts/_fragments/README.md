# shared fragments

Reusable prompt sections injected via `{{fragment:name}}` placeholders.

## fragments

- `safety_rules` — content safety constraints and refusal rules
- `style_and_language_rules` — output style, tone, and locale handling

