# ─── Single consolidated .env at monorepo root ───
# Copy this to .env and fill in your values.
# Both packages/backend/.env and packages/frontend/.env are symlinks to this file.

# database
DATABASE_URL=postgresql://YOUR_USER@localhost:5432/aitourguide

# openai
OPENAI_API_KEY=sk-your-openai-api-key

# oauth - google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# oauth - apple
APPLE_CLIENT_ID=your-apple-client-id
APPLE_TEAM_ID=your-apple-team-id

# jwt
JWT_SECRET=your-jwt-secret-change-in-production

# server
PORT=4000
HOST=0.0.0.0
NODE_ENV=development

# frontend api url (for physical device testing, set to your Mac's local IP)
# EXPO_PUBLIC_API_URL=http://192.168.x.x:4000

# tts
# Voice options: alloy, ash, ballad, coral, echo, fable, nova, onyx, sage, shimmer
# Recommended for tour guide tone: nova (warm, engaging)
# Model options: tts-1 (fast), tts-1-hd (better quality), gpt-4o-mini-tts (best, supports instructions)
# Note: non-English locales auto-upgrade to tts-1-hd via locale-accents config.
TTS_AUDIO_DIR=./audio-cache
TTS_VOICE=nova
TTS_MODEL=tts-1
TTS_SPEED=1.0

# llm
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.7
LLM_VISION_TEMPERATURE=0.3

